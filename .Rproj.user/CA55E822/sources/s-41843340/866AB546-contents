isolate_data <- function(data, id, cols) {
  ### This function takes a section of the data frame for processing
  ### data: the full data frame
  ### id: the column number (or numbers) for participant identifier(s)
  ### cols: columns for the measure of interest
  d0 <- data[c(id, cols)]
  return(d0)
} 


